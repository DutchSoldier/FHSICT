CREATE TABLE "CATEGORIE" (
  "NAAM" VARCHAR2(64 BYTE) NOT NULL,
  "OMSCHRIJVING" VARCHAR2(2048 BYTE) NULL)
/
CREATE UNIQUE INDEX "PK_CATEGORIE"
  ON "CATEGORIE" ( 
    "NAAM")
/
ALTER TABLE "CATEGORIE" ADD ( 
  CONSTRAINT "PK_CATEGORIE" 
    PRIMARY KEY ( "NAAM") 
    USING INDEX "PK_CATEGORIE" 
    ENABLE 
    VALIDATE )
/
